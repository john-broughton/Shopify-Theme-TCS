{%- render 'section-apps' -%}

{% schema %}
{
  "name": "t:labels.apps",
  "tag": "section",
  "settings": [
    {
      "type": "checkbox",
      "id": "full_width",
      "label": "t:labels.full_page_width",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "space_around",
      "label": "t:actions.add_spacing_above",
      "default": true
    }
  ],
  "blocks": [
    {
      "type": "@app"
    }
  ],
  "presets": [
    {
      "name": "t:labels.apps"
    }
  ]
}
{% endschema %}
